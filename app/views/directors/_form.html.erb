<%= form_with(model: director, local: true) do |form| %>
  <% if director.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
        <% director.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mb-3">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field mb-3">
    <%= form.label :career, class:"form-label" %>
    <%= form.text_area :career, class:"form-control" %>
  </div>

  <div class="field mb-3">
    <%= form.label :awards, class:"form-label" %>
    <%= form.text_area :awards, class:"form-control" %>
  </div>

  <div class="field mb-3">
      <%= form.label :image, class:"form-label" %>
      <%= form.file_field :image, class:"form-control" %><br>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
